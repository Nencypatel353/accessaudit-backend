# Currently the app runs with zero required environment variables --
# it uses local SQLite (accessaudit.db) and no external services.
# This file is a placeholder for when you add things like:

# DATABASE_URL=postgresql://user:password@localhost:5432/accessaudit
# JWT_SECRET_KEY=change-me
# FRONTEND_ORIGIN=http://localhost:4200
